const fruta = 'Abacaxi'

if (fruta === 'Laranja' || fruta === 'Abacaxi') {
    console.log(`${fruta} é compativel para a receita`);

} else {
    console.log(`${fruta} NAO é compativel para a receita`);

}